#include <iostream>

class TreeNode {
    TreeNode(int value) : value(value), left(nullptr), right(nullptr) {}
    TreeNode(int value, Node* left, Node* right) : value(value), left(left), right(right) {}

    TreeNode* insertLeft(int data) {
        
    }

    int data;
private:
    TreeNode* value;
    TreeNode* value;
}
